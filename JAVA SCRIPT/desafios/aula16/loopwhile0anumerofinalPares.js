//Escreva um algoritmo que exiba todos os números pares de 0
//  ao número passado pelo usuário (utilize prompt).
let num = 0
              let limite = Number(prompt('Digite o número final para a contagem'))

              while(num<=limite){
                  console.log(num)
                  num+=2 // incrementando de dois em dois para garantir apenas os pares
              }
            
          